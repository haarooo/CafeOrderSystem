<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.cafeordersystem.order.read.ReviewableOrderReadMapper">

    <select id="existsReviewableOrder" resultType="int">
        SELECT COUNT(*)
        FROM reviewable_order_read
        WHERE order_id = #{orderId}
    </select>

    <insert id="upsertOrder"
            parameterType="com.example.cafeordersystem.order.read.ReviewableOrderReadRow">
        INSERT INTO reviewable_order_read (
        order_id,
        order_price,
        created_at,
        review_written
        )
        VALUES (
        #{orderId},
        #{orderPrice},
        NOW(6),
        FALSE
        )
        ON DUPLICATE KEY UPDATE
        order_price = VALUES(order_price),
        created_at = VALUES(created_at)
    </insert>

    <update id="markReviewWritten">
        UPDATE reviewable_order_read
        SET review_written = TRUE
        WHERE order_id = #{orderId}
    </update>

</mapper>